import { wrapRootElement as wrap } from "./root-wrapper";
import { onInitialClientRender as initial } from "./onInitialClientRender";

export const wrapRootElement = wrap;
export const onInitialClientRender = initial;
