import * as React from "react";

const NotFoundPage = () => {
    return (
        <main>
            <div className="page">
                <h1>Page not found</h1>
            </div>
        </main>
    );
};

export default NotFoundPage;
